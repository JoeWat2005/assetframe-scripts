fpdf2>=2.8
pymupdf>=1.26

# IANA time-zone database for zoneinfo. The OCI Linux box ships a system tz database, but
# pinning the pure-data tzdata package guarantees sessions.py can compute DST-correct exchange
# session boundaries on any host (incl. bare Windows dev boxes) and keeps the tz data updatable.
tzdata>=2024.1

# Postgres client (psycopg3) for the OCI engine runner — scripts/engine_ops.py,
# poller.py, scheduled_run.py poll/write the Neon coordination tables (generation_requests,
# engine_runs, engine_state). The [binary] extra ships a prebuilt libpq so no system pq-dev
# is needed on the VM. (sync-db.mjs uses the Node Neon driver; Python had no PG client before.)
psycopg[binary]>=3.1

# Autonomous brief authoring + adversarial critic (scripts/brief_writer.py, critic.py).
# Uses the Anthropic Messages API with the server-side web_search tool. A keyless run
# degrades gracefully to the operator-written brief flow, so this is only required when
# ASSETFRAME_AUTHOR_BRIEFS is on AND ANTHROPIC_API_KEY is set.
anthropic>=0.40

# Cloudflare R2 upload client for scripts/publish.py. REQUIRED on the autonomous engine
# box: the daily run's publish step (engine_ops._publish_chain) pushes Pro/free files to
# R2, and without it publish exits 2 ("boto3 is required") so the generate -> publish ->
# sync chain halts before anything reaches R2 or Neon. (Local free-only builds never call
# publish, but installing it is harmless.)
boto3>=1.34

# JWT validation for the box control server (scripts/coordination/control_server.py): verifies the
# Cloudflare-Access `Cf-Access-Jwt-Assertion` JWT (RS256) against the team JWKS before any /control
# command runs. The [crypto] extra pulls in `cryptography` for RS256. Only needed on the box running
# the control service; the rest of the engine never imports it.
PyJWT[crypto]>=2.8
